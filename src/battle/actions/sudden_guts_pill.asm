
BTLACT_SUDDEN_GUTS_PILL: ;$C2AA7F
	REP #PROC_FLAGS::ACCUM8 | PROC_FLAGS::INDEX8 | PROC_FLAGS::CARRY
	RESERVE_STACK_SPACE_CLOBBER 22
	JSR a:.LOWORD(FAIL_ATTACK_ON_NPCS)
	CMP #$0000
	BNE @UNKNOWN1
	LDX .LOWORD(CURRENT_TARGET)
	LDA a:battler::guts,X
	ASL
	CMP #$00FF
	BCC @UNKNOWN0
	LDA #$00FF
@UNKNOWN0:
	LDX .LOWORD(CURRENT_TARGET)
	STA a:battler::guts,X
	LOADPTR TEXT_BATTLE_GUTS_BECAME, $0E
	LDX .LOWORD(CURRENT_TARGET)
	LDA a:battler::guts,X
	STA $06
	STZ $08
	MOVE_INT $06, $12
	JSL DISPLAY_TEXT_WAIT
@UNKNOWN1:
	PLD
	RTL
